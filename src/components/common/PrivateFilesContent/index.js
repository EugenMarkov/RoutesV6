import Typography from "@mui/material/Typography";

/**
 * @desc function for create private files container
 * @returns {JSX.Element}
 */
const PrivateFilesContent = () => {
  return (
    <section>
      <Typography variant="h3">Private files</Typography>
      <div>Files</div>
    </section>
  );
};

export default PrivateFilesContent;
