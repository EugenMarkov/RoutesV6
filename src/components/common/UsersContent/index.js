import Typography from "@mui/material/Typography";

/**
 * @desc function for create users container
 * @returns {JSX.Element}
 */
const UsersContent = () => {
  return (
    <section>
      <Typography variant="h3">Users</Typography>
      <div>Table with user's data</div>
    </section>
  );
};

export default UsersContent;
